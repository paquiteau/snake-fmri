# @package _global_

hydra:
  job:
    chdir: true
  mode: MULTIRUN
  sweeper:
    params:
      simulation.handlers.noise.snr:  10, 30, 100, 1000
      simulation.handlers.acquisition.accel: 1, 2, 4, 8

  sweep:
    dir: multirun/extend_sweep/${now:%Y-%m-%d}/${now:%H-%M-%S}
